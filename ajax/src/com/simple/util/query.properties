hr.getAllDepartments=select * \
from departments \
order by department_id asc

hr.getEmployees=select * \
from employees \
where department_id = ? \ 
order by department_id asc

hr.getEmployeeById=select * \
					from employees \
					where employee_id = ?